# Dependencies
node_modules/

# Build output
dist/
release/
electron-dist/
electron-dist2/

# TypeScript build cache
*.tsbuildinfo

# Vite temp
vite-err.txt
vite-out.txt

# ONNX models (too large for git, distributed via extraResources)
public/oonx/

# Piper TTS assets (copied from node_modules by postinstall)
public/piper-tts/

# Sherpa-ONNX WASM assets (copied from node_modules by postinstall)
public/sherpa-onnx-wasm/

# SoundFont files (binary assets, user-provided)
public/sf2/

# Songsee binary (built locally, too large for git)
bin/songsee/songsee.exe
bin/songsee/songsee

# User work files
work/
test denoise/

# Audio/MIDI collections (user data, not source code)
music collection/
soundbank/
midi_bank/

# Films used to exercise the video nodes: 124 MB of user media, not source code.
# A `git add -A` swept them into the index once; ignoring them puts that out of reach.
video example/

# Presets (user-generated, kept locally)
presets/

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Environment / secrets
.env
.env.*

# Claude / opencode internal. Le contrat de notice est VERSIONNE : il vaut pour le depot,
# pas pour une machine. Le reste de .claude/ reste personnel.
.claude/*
!.claude/settings.json

# Python temp files
[object Promise]/
python-path.txt

# Stray npm wrapper
npm

# Logs
*.log
npm-debug.log*
dev-server.err

# Debug / stray files
$null
test-sa3-cjs.cjs

# Build scripts/output (not source)
build/

# Exception: the custom NSIS installer script is source, not output.
!build/installer.nsh

# Build artifacts
release-build/
release-prod/

# Test outputs
test-results/

# Tesseract OCR models (downloaded at build/setup)
eng.traineddata

# Temporary audit scripts
scripts/audit-params.mjs

# Duplicate terms file (TERMS_OF_USE.txt is the source)
TERMS_OF_USE.md

# Generated / internal docs (not distributed)
# COMPONENTS.md est versionné : le README y renvoie, et un test vérifie qu'il
# correspond au registre (src/docs/generer-catalogue.test.ts).
EXERCISE-WORKBOOK.md

# UNE INSTALLATION D'ATTIC DANS LE DOSSIER DES SOURCES.
#
# La racine du dépôt contient une application Attic installée — `Attic.exe`, son désinstalleur, les
# `.pak` et les DLL de Chromium, `locales/`, et un dossier `resources/` de 2,7 Go. Ce n'est pas une
# sortie de build : c'est l'installeur qui a été pointé sur `E:\attic`.
#
# Deux conséquences, et la seconde est la plus sournoise : `git status` mélange ces fichiers avec le
# travail en cours — un `git add -A` distrait committerait `Attic.exe` —, et le LINTER PARCOURAIT
# les copies de sources dans `resources/app/`, signalant des défauts à un endroit où les corriger
# n'aurait servi à rien.
#
# Les ignorer évite l'accident. Cela ne remplace pas de désinstaller l'application d'ici.
resources/
resources.pak
Attic.exe
Uninstall Attic.exe
locales/
*.pak
*.dll
*.bin
icudtl.dat
LICENSE.electron.txt
LICENSES.chromium.html
vk_swiftshader_icd.json
/version
